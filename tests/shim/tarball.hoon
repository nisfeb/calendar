::  tarball, as much of grubbery's as the calendar libs name: a rail is a
::  file's directory and name (grubbery desk/lib/tarball.hoon, +rail).
::  Only the test desk has this; on a ship grubbery supplies the real one.
|%
+$  rail  [=path name=@ta]
--
